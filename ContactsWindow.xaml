<Window x:Class="LogCollector.ContactsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="联系人" Height="400" Width="600">
    <Grid>
        <ListView x:Name="ContactsListView" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="姓名" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="电话" DisplayMemberBinding="{Binding Phone}" Width="150"/>
                    <GridViewColumn Header="电子邮件" DisplayMemberBinding="{Binding Email}" Width="250"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
